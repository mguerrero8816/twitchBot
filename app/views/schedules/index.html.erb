<table>
  <% @schedules.each do |schedule| %>
    <tr>
      <td><%= schedule.title %></td>
      <td><%= schedule.description %></td>
      <td><%= schedule.start_at %></td>
    </tr>
  <% end %>
</table>
